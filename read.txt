F4 is the default keybind. Press it to open Candlelight.

You need the following modules:
pyautogui
pynput
cefpython3
psutil

Candlelight 1.1.0b2

Builtin Packages
+ Added the `use` keyword to `pkg`. It simply enables a package.
+ Added the `util` package.
+ Added the `hk` package.

Other
+ Added an update notifier.